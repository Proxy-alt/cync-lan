Thanks for the detailed pushback — that's a fair objection, and I agree unconditional discovery is the wrong trade for every other device on the bus.

I've written the lazy-verify version instead: `discover_descs()` still synthesizes the `0x2902` exactly as it does today, but `register_notify()` now issues a single-handle `FIND_INFORMATION` for that handle before it ever writes to it, and only proceeds with the CCC write if the peer confirms `0x2902` there. If the peer answers with anything else (or doesn't answer), `ccc_handle` is cleared and this reaches the same "no CCC" path `register_notify()` already handles correctly today. That's the extra round trip only on first `register_notify()` for a characteristic whose CCC was never actually discovered — a real, discovered CCC is untouched by this patch and costs nothing extra.

Patch attached: `0002-lazy-verify-ccc-before-writing.patch`.

This time I built and ran it as `bluetoothd` itself, as you asked. Applied cleanly to the 5.86 release tree (matching this adapter's running version), compiled, and ran in place of the system daemon against the actual onboard BCM4345C0: it powered `hci0` normally and discovered the live mesh — dozens of devices across both OUI families from the report, `F4:BC:DA` and `78:6D:EB`.

What I don't have yet is a clean connect-and-subscribe trace through it. Running as a fresh `bluetoothd` meant losing this adapter's usual paired/cached state, and on this multi-device mesh that showed up as connection instability before GATT discovery finished — MTU exchange completing but primary service discovery failing outright, a few times in a row, before I called it. It never got far enough into a session to reach `discover_descs()` or `register_notify()` either way, so I don't want to claim a trace I don't actually have. This looks like the adapter's known multi-connection contention on this system rather than anything the patch touches, but I can't rule that out for certain from what I saw.

So: build-verified against your own binary this time, confirmed running live against the real controller and the real mesh, but not yet a traced repro of the fix in action. Happy to try again with more runway, or take direction on a more targeted setup (a single already-bonded device, a longer window) given the connection contention I hit.
