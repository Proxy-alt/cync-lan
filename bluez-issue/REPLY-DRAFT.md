Thanks for the detailed pushback — that's a fair objection, and I agree unconditional discovery is the wrong trade for every other device on the bus.

I've written the lazy-verify version instead: `discover_descs()` still synthesizes the `0x2902` exactly as it does today, but `register_notify()` now issues a single-handle `FIND_INFORMATION` for that handle before it ever writes to it, and only proceeds with the CCC write if the peer confirms `0x2902` there. If the peer answers with anything else (or doesn't answer), `ccc_handle` is cleared and this reaches the same "no CCC" path `register_notify()` already handles correctly today. That's the extra round trip only on first `register_notify()` for a characteristic whose CCC was never actually discovered — a real, discovered CCC is untouched by this patch and costs nothing extra.

Patch attached: `0002-lazy-verify-ccc-before-writing.patch`.

This time I built and ran it as `bluetoothd` itself, as you asked. Applied cleanly to the 5.86 release tree (matching this adapter's running version), compiled, and ran in place of the system daemon against the actual onboard BCM4345C0: it powered `hci0` normally and discovered the live mesh — dozens of devices across both OUI families from the report, `F4:BC:DA` and `78:6D:EB`.

I made a second attempt at a real connect-and-subscribe trace and narrowed the cause, but still don't have that trace, so I want to be precise about what actually happened rather than round it off.

I run this build inside a container (a management add-on with D-Bus/adapter access, not a full host shell), and every connection attempt from it — across multiple real devices, both OUI families, RSSI down to -76 dBm so it isn't signal quality — fails one of two ways: either the ATT socket connect itself returns `Function not implemented` (`ENOSYS`) in `att_connect_cb()` right after the HCI-level LE connection succeeds, or (less often) it gets past that and MTU exchange completes, then `discover_primary_cb()` fails immediately with `ATT ECODE: 0x00` — a local abort, nothing on the wire. Neither ever reaches `discover_descs()` or `register_notify()`. Immediately after, I confirmed the real host-native `bluetoothd` does not have this problem: restarting it, Home Assistant's own Bluetooth stack reconnected through it and completed a normal GATT write to an unrelated BLE device with no issue. So this looks like a capability/seccomp gap specific to opening new L2CAP CoC sockets from inside this particular build container, not the adapter, not the mesh, and not this patch — I just don't have a way to rule that out with certainty from here, and don't want to claim a trace I don't have.

A third attempt, specifically to rule out "maybe it's this device": my Home Assistant BLE integration for these devices keeps its own list of mesh entry points it has proven it can actually complete a full GATT session against — right now that list has exactly one address. I retried against that specific node. Same failure, same signatures.

A fourth attempt, and I want to correct what I said above rather than let it stand. I got genuinely unconstrained access — not a container, root on the host's Bluetooth stack directly — and reran the same build there. It immediately completed a full GATT session against a different BLE device on this host (services, characteristics, and descriptors, including a real discovered `0x2902` on that device), on the first attempt. So the capability/seccomp theory was wrong; I retract it. But connecting specifically to the Cync mesh nodes was still unreliable even with that unconstrained access: 4 attempts total, across two of the OUI-family nodes, all failed — a mix of the same `ENOSYS` and post-MTU-exchange local aborts as before. None of the four ever reached `discover_descs()` or `register_notify()`.

At this point I think that's telling me something about these specific nodes rather than about my setup: my own original report to this issue already recorded "outcome varies by attempt on the same device and the same firmware... every one ended in UNLIKELY_ERROR" against the *real* host daemon, across 14 attempts. Connection establishment to this Telink hardware looks genuinely marginal on this adapter, independent of which `bluetoothd` binary is driving it.

So, current state: build-verified against your own binary, confirmed powering the real adapter and completing full GATT sessions (proven against another device, not this one) with unconstrained host-level access, but still no traced repro of the fix in action against a Cync node specifically — and I now think that's connection-establishment flakiness in the mesh itself, not something further debugging of my test setup will fix. I can keep trying opportunistically, but I don't want to keep asserting causes I haven't verified. Happy to take direction, or if you're able to exercise this yourself against the traces/hardware access already on the issue.
