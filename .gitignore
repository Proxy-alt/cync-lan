certs
raw_mesh.cync
cync_mesh.yaml
/docs/debug.md
./config
uuid.txt
./tailwindcss
docker-compose.yaml
uv.lock
dump.txt
*.egg-info
__pycache__/
*.pyc
.coverage
.pytest_cache/
.DS_Store

# setuptools build output. build/lib/ was committed at some point and then
# quietly diverged from src/ - it still carried the old hand-written
# __version__ after src/ stopped using one - so it was 15 files of duplicate
# source that only ever misled a grep.
build/
dist/
